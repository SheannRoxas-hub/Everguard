<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Everguard" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Make system bars transparent -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Assign the Input Style -->
        <item name="textInputStyle">@style/Widget.Everguard.TextInputLayout</item>

        <!-- Assign Primary Colors -->
        <item name="colorPrimary">@color/everguard_blue_dark</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Set Default Font for the whole app -->
        <item name="fontFamily">@font/poppins_medium</item>
    </style>

</resources>